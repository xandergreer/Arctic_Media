# copy to .env and tweak
ENV=dev
DEBUG=true
SECRET_KEY=change_me_in_prod
DATABASE_URL=sqlite+aiosqlite:///./arctic.db

HOST=0.0.0.0
PORT=8000

# Optional: port used only on first-ever startup (when no server settings exist)
# Helps avoid conflicts with commonly used ports like 8000, 8080, 8096, etc.
FIRST_RUN_PORT=8085

# Built-in TLS (no reverse proxy)
# Set these to serve HTTPS directly from the app
# Use absolute paths to your certificate and private key files
SSL_ENABLED=false
SSL_CERT_FILE=
SSL_KEY_FILE=

FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

TMDB_API_KEY=8eb7615054d470eaeb82adc7e4a67707
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:8000
